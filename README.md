# dsb-mail-receipt-to-pdf

To extract receipts from DSB sent via mail for accounting purposes


## Dependencies

Install `wkhtmltopdf`.

Install Python packages:

```
pip install -r requirements.txt
```